type TextSelectionProps = {
  textNode: HTMLElement;
  selection: {
    start: number;
    end: number;
  };
}

export const TextSelection = ({
  textNode,
  selection,
}: TextSelectionProps) => {


  return (
    <></>
  )
}